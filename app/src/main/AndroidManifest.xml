<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uratio.testdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".parse.XmlParseActivity"></activity>
        <activity android:name=".animlist.ListViewActivity" />
        <activity android:name=".view.ViewStubActivity" />
        <activity android:name=".load.IndicatorActivity" />
        <activity android:name=".load.LoadAnimActivity" />
        <activity android:name=".animlist.AnimRcvActivity" />
        <activity android:name=".animlist.AnimListActivity" />
        <activity android:name=".img.LongFigureActivity" />
        <activity android:name=".designmodel.DesignModelActivity" />
        <activity android:name=".arithmetic.ArithmeticActivity" />
        <activity android:name=".workmanager.WorkManagerActivity" />
        <activity android:name=".PickersActivity" />
        <activity android:name=".hexagon.HexagonActivity" />
        <activity android:name=".TitleChangeActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".RcvActivity" />
        <activity
            android:name=".ThirdActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="landscape" />
        <activity
            android:name=".SecondActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 使用临时文件安装apk -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.uratio.testdemo.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>